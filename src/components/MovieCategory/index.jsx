import React from 'react';

const MovieCategory = props => {
    this.props = props;
    return (
        <div className="MovieCard__Category">{this.props.cat}</div>
    );
};

export default MovieCategory;